<div class="container">
  <div class="logo">
    <%= image_tag "Label.png" %>
  </div>
  <div class="row">
    <%= render "layouts/search", q: @q %>
  </div>
  <div class="row">
    <%= render "layouts/tags", tags: @tags %>
  </div>
  <div class="row">
    <% @post_images.each do |post| %>
      <% if post.is_active == true %>
      <div class="image-item mt-4 shadow-lg">
        <%= link_to post_image_path(post) do %>
          <%= attachment_image_tag post, :post_image, size:"200x200" %>
        <% end %>
      </div>
      <% end %>
    <% end %>
  </div>
</div>

